require('Comment').setup({
    padding = true,
    sticky = true,
    toggler = {
        ---Line-comment toggle keymap
        line = 'gcc',
        ---Block-comment toggle keymap
        block = 'gbc',
    },
})
